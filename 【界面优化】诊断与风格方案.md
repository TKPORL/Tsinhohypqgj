# 界面优化：诊断与风格方案

> 生成时间：2026-09-26
> **状态：✅ 已全部实施（2026-09-26，见【变更记录】v19）** —— 方向选定 **A · 深色 + 粉紫**，8 项优化全部落地，主界面与导出 HTML 两套皮肤同步替换。
> 参考截图：`docs/风格存档/新版-深色粉紫截图/`；被替换的旧风格：`docs/风格存档/旧版-Origin深色编辑风/`
>
> 范围：主界面（`acg_crawler/static/style.css` + `templates/index.html` + `static/app.js`）与离线导出 HTML（`acg_crawler/generator/__init__.py`）
> 结论：**风格问题出在「排版规则」，不在「深色」**；功能侧有 8 处可优化，其中 2 处是明确的 bug 级问题。

---

## 一、风格诊断：为什么现在看着"怪"

当前风格是 2026-09-23 换上的「Origin 深色编辑风」，照着一家英文财经网站的排版语言做的。它本身不丑，但**四条规则用在中文工具上是错配**：

### 1. 中文被当成英文排版（最主要的"怪"）

`style.css` 里几乎所有小标签都套了同一组规则：

```css
font-family: var(--font-mono);        /* Roboto Mono 等宽 */
font-size: 11px;
letter-spacing: 0.182em;              /* 字距拉开 0.182 字 */
text-transform: uppercase;            /* 大写 */
```

中文没有大小写，`uppercase` 对它完全无效；但 `letter-spacing: 0.182em` 是实打实生效的。结果就是「目 标 站 点」「爬 取 模 式」「速 度 档 位」——每个字之间被撑开一大截，读起来松散、别扭。

**受影响的元素**：`.form-group label`、`.nav-btn`、`.panel-header h2` 附近的所有 mono 标签、`.history-table th`、`.stat-label`、`.site-page-range`、`.export-section h3`、`.cleanup-name`、`.sticky-label`、`.plat-tab`、`.copy-text`、`.tag` 等 —— 基本覆盖全站。

### 2. 中文大标题掉到了衬线字体上

```css
--font-display: 'Playfair Display', 'Noto Serif SC', Georgia, 'Songti SC', 'SimSun', serif;
```

Playfair Display 是纯英文字体，**没有中文字形**。所以「爬取控制」「爬取结果」这些 38px 大标题实际渲染的是 `Noto Serif SC` 或 `SimSun`——细笔画、带衬线，像杂志印刷品。对一个资源浏览工具来说，这是最不搭的部分。

### 3. 满屏英文装饰

- `header h1::before` 硬编码了 `content: 'LOCAL ARCHIVE · 2026'`
- 导出卡片图标写死 `PC` / `PC+安卓`（英文 + 等宽 + 字距）
- 所有标签、按钮、导航统一大写

一个纯中文的本地工具，塞英文装饰会产生"洋气但不是我"的疏离感。

### 4. 冷色调 + 大色块抢戏

- 主色是纯白 + 灰阶 + `iris #847dff` / `orchid #dd90d8`，整体偏冷、偏"性冷淡"
- 结果页三条满幅色块（`.date-group-header[data-plat]`）面积很大，颜色比游戏封面还抢眼，**妨碍快速找游戏**
- 图片为主的页面，UI 再压一层冷灰，封面会显得灰扑扑

**一句话**：深色本身没问题（看图的工具就该深色），要改的是上面这四条。

---

## 二、三个风格方向

| 方向 | 说明 | 优点 | 代价 |
|---|---|---|---|
| **A · 深色 + 粉紫** | 回到 `docs/风格存档/旧版-粉紫渐变/` 的路子，但重做质感 | 最贴二次元资源站气质，暖、有辨识度；也是你之前想换回的 | 需要重写一遍 CSS（主界面 + 导出两套） |
| **B · 深色 + 中性** | 色调基本不动，只修上面四条错配 | 改动最小，风格克制、像正经工具 | 偏冷，和"黄油资源"这个题材的调性弱一些 |
| **C · 浅色 + 亮紫** | 整体换成浅色 | 白天看着清爽干净 | 封面图不突出；和"深夜看图"的使用场景不搭 |

**推荐 A**。理由：这是个以图片为主的资源工具，深色对图片最友好；而"怪"的根因是排版，不是颜色——把中文字距、衬线标题、英文装饰这三件事改掉，即使保留现在的深色也会立刻正常。A 在修掉这些的基础上，把主色换回暖色，最贴合题材。

### A 方向的具体改法（如果选 A）

1. **去掉中文上的 `uppercase` 和 `letter-spacing`**（中文标签字距回到 `0` 或 `0.02em`）；英文标签（`PC` / `LIKE` 这类）保留字距
2. **中文标题改用无衬线中文字重字体**：`'PingFang SC', 'Microsoft YaHei', 'Noto Sans SC'`，`font-weight: 600`；删掉 Playfair / 宋体回退
3. **删掉英文装饰**：`h1::before` 的 `LOCAL ARCHIVE · 2026` 换成中文或直接删
4. **主色换成 `#e056a0`（粉红）**，用于选中态、主按钮、标签强调；紫 `#8b5cf6` 做渐变搭配（`linear-gradient(135deg,#e056a0,#8b5cf6)`）
5. **底色回 `#0e0e14`，卡片 `#14141e`**，圆角 12px，卡片 hover 轻微上浮
6. **三条大色块分组头收窄**：改成一条细的彩色标签条，不再占满一行

> ⚠️ 两套皮肤必须同步改：主界面改 `static/style.css`，导出 HTML 改 `generator/__init__.py` 里 `HTML_TEMPLATE` 的 `<style>` 段，否则两边会不一致。

---

## 三、功能 / 交互优化清单

按「值得做的顺序」排，前两条属于明确的问题：

### 1. 日期默认值写死了（问题级）

`templates/index.html` 第 130 / 132 行：

```html
<input type="date" id="startDate" value="2026-09-01">
<input type="date" id="endDate" value="2026-09-06">
```

今天是 2026-09-26，打开页面默认就是三周前的过期区间。**应改为「今天往前 7 天 ~ 今天」动态生成**，或在 `app.js` 初始化时按当前日期填。

### 2. 站点区占用过大，且站点写死在 HTML（问题级）

6 个站点 × (复选框 + 起始页 + 结束页) = 12 个数字输入框，占掉爬取面板大半屏。
且站点列表是**硬编码在 HTML 里**的，`config.yaml` 里增减站点后前端不会自动跟着变。

建议：改成每站一行紧凑布局（站点名 + 页码折叠收起），或由后端 `/api/sites` 吐出列表、前端渲染。

### 3. 结果页顶部一行塞了 6 个控件

来源下拉 + 搜索框 + 4 个平台 tab + 导出按钮 + 条数统计，全挤在一行。建议分两行：第一行筛选（来源 / 平台 / 搜索），第二行操作（导出 / 计数）。

### 4. 三条大色块分组头太占地方

即使折叠状态也占满一整行。建议收成细标签条（见风格方案第 6 条）。

### 5. 删除入口分散在 4 处

- 结果页 · 单张卡片删除
- 结果页 · 批量删除
- 历史记录 · 删除任务（含帖子）
- 导出文件页 · 按批次删除

粒度不同（帖子 / 批次），功能上说得通，但**入口分散，找起来累、也容易误删**。建议至少把「导出文件页的批次删除」并入「数据管理」，删除统一收口。

### 6. 历史记录 9 列表格窄屏会撑破

`ID / 类型 / 站点 / 状态 / 成功 / 跳过 / 失败 / 时间 / 操作` 共 9 列。900px 断点下只是导航改横排，表格没做处理。建议窄屏隐藏「类型 / 跳过 / 失败」等次要列，或改卡片式。

### 7. 「重新下载图片」放错位置

它和「开始爬取 / 停止爬取」并排放在同一个操作区，但前者是**维护动作**，后者是**任务动作**，放一起容易点错。建议移到「数据管理」面板。

### 8. 缺一个总览

现在「库里多少条」「占多少磁盘」散在导出页（记录数）和数据管理页（清理项）两处。建议在爬取控制面板顶部加一条状态行：`共 N 条 · 占用 X GB · 最近一次爬取 <时间>`。

---

## 四、改动落点速查

| 要改的东西 | 文件 |
|---|---|
| 主界面视觉 | `acg_crawler/static/style.css` |
| 主界面结构（DOM） | `acg_crawler/templates/index.html` |
| 主界面交互（日期默认值等） | `acg_crawler/static/app.js` |
| 导出 HTML 视觉 | `acg_crawler/generator/__init__.py` → `HTML_TEMPLATE` 的 `<style>` 段 |
| 站点增减 | `acg_crawler/config.yaml`（改完记得同步前端列表） |
| 旧版风格参考 | `docs/风格存档/旧版-粉紫渐变/` |

---

## 五、待确认

1. 风格走 **A（深色+粉紫）** / **B（深色+中性）** / **C（浅色）**？
2. 主界面和导出 HTML 是否**同时**改？（不改的话两边会不一致）
3. 上面 8 条优化，是**全做**，还是先做 1、2 两条问题级的？
