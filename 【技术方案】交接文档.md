# 【技术方案】交接文档

类型：项目开发

## 〇、协作约定
本文档用于「交接文档驱动」的跨对话协作，所有状态以本文档为准。
- 用户说"阅读交接文档，准备开始"或意思相近的说法 → 按开场协议执行
- 用户说"对话结束，总结项目"或意思相近的说法 → 按收尾协议执行
- 开场协议：①用3~5行概括对目标/进度/关键结论的理解 ②列出下一步待办并给建议 ③有明确风险才单列
- 收尾协议：①把一~八板块更新到最新状态 ②把本次对话中用户新告知的信息追加到「九、对话信息记录」

## 一、背景与目标

### 项目定位
本地运行的ACG黄油资源聚合爬取工具，从4个网站抓取游戏资源，生成可离线浏览的HTML卡片页面。

### 目标网站（5个）
| 序号 | 网站 | 域名 | CMS | 爬取难度 |
|------|------|------|-----|----------|
| 1 | ACG游戏姬 | www.acgyxjvip.com | WordPress + Lolimeow | ★☆☆ 低 |
| 2 | 萌幻ACG | bbs.acgrx.com (备用: bbs4.acgrx.com) | Typecho + MiKu | ★☆☆ 低 |
| 3 | ACG图书馆 | acgll.xyz | WordPress + Zibll 8.8 | ★★☆ 中 |
| 4 | ACG俱乐部 | www.acgjlb.cc | 待确认 | ★☆☆ 低 |
| 5 | ~~IACG.RIP~~ | ~~community.acg-c.net~~ | ~~Flarum~~ | ~~★★★ 高~~ **暂不爬取** |

### 核心功能
1. 按日期爬取 / 按页码爬取
2. 只保留含百度网盘或移动云盘链接的帖子
3. 图片下载到本地，HTML离线可用
4. 按点赞+评论排序（萌幻ACG无数据则随机）
5. 生成 PC.html 和 PC+安卓.html
6. Web界面：实时进度、卡片展示、历史记录

## 二、技术架构

### 整体架构
```
┌─────────────────────────────────────────────────────┐
│                    前端 (原生HTML/JS/CSS)              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────┐ │
│  │ 爬取控制  │  │ 进度展示  │  │ 结果卡片  │  │ 导出 │ │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └──┬───┘ │
│       │              │              │            │     │
├───────┼──────────────┼──────────────┼────────────┼─────┤
│       └──────────────┼──────────────┘            │     │
│                      ▼                           ▼     │
│              ┌──────────────┐            ┌───────────┐ │
│              │  Flask 后端   │            │ SQLite DB │ │
│              └──────┬───────┘            └───────────┘ │
│                     │                                 │
├─────────────────────┼─────────────────────────────────┤
│                     ▼                                 │
│  ┌──────────────────────────────────────────────────┐ │
│  │              爬虫引擎 (多线程)                     │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────┐ │ │
│  │  │ACG游戏姬 │ │ 萌幻ACG  │ │ACG图书馆 │ │代理 │ │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └─────┘ │ │
│  └──────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

### 技术栈
| 组件 | 选型 | 理由 |
|------|------|------|
| 后端 | Python 3.9+ / Flask | 需求指定，轻量 |
| 前端 | 原生HTML/JS/CSS | 需求指定，无构建工具 |
| 数据库 | SQLite | 本地文件，零配置 |
| 爬虫 | requests + BeautifulSoup4 | 静态页面解析 |
| 图片下载 | requests (流式) | 支持大文件 |
| 并发 | ThreadPoolExecutor | 多线程爬取 |
| 模板 | Jinja2 | HTML生成 |
| 代理 | HTTP代理 (7890端口) | 统一走Clash |

### 目录结构
```
acg_crawler/
├── start.bat                  # 一键启动脚本
├── requirements.txt           # Python依赖
├── app.py                     # Flask主程序
├── config.yaml                # 配置文件
├── crawler/
│   ├── __init__.py
│   ├── base.py               # 基础爬虫类
│   ├── acgyxj.py             # ACG游戏姬爬虫
│   ├── acgrx.py              # 萌幻ACG爬虫
│   ├── acgll.py              # ACG图书馆爬虫
│   └── anti_spam.py          # 反反爬模块
├── parser/
│   ├── __init__.py
│   ├── html_parser.py        # HTML解析器
│   ├── image_handler.py      # 图片下载处理
│   └── link_extractor.py     # 网盘链接提取
├── database/
│   ├── __init__.py
│   ├── models.py             # 数据模型
│   └── db.py                 # 数据库操作
├── generator/
│   ├── __init__.py
│   └── html_generator.py     # HTML生成器
├── templates/
│   ├── index.html            # 主界面
│   ├── card.html             # 卡片模板
│   └── static/
│       ├── style.css
│       └── app.js
├── output/                   # 生成的HTML文件
│   ├── PC.html
│   └── PC+安卓.html
├── images/                   # 下载的图片
│   ├── acgyxj/
│   ├── acgrx/
│   └── acgll/
└── data/
    └── crawler.db            # SQLite数据库
```

## 三、数据模型

### posts 表
```sql
CREATE TABLE posts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    source TEXT NOT NULL,           -- 来源站点: acgyxj/acgrx/acgll
    source_id TEXT NOT NULL,        -- 原站帖子ID
    source_url TEXT NOT NULL,       -- 原帖链接
    title TEXT NOT NULL,            -- 标题
    platform TEXT DEFAULT 'unknown', -- 平台: pc/android/pc_android/unknown
    content TEXT,                   -- 正文内容(纯文本)
    likes INTEGER DEFAULT 0,       -- 点赞数
    comments INTEGER DEFAULT 0,    -- 评论数
    views INTEGER DEFAULT 0,       -- 浏览量
    unzip_code TEXT,               -- 解压码
    cheat_code TEXT,               -- 作弊码/解压步骤
    baidu_link TEXT,               -- 百度网盘链接
    baidu_code TEXT,               -- 百度提取码
    mobile_link TEXT,              -- 移动云盘链接
    mobile_code TEXT,              -- 移动云盘提取码
    images TEXT,                   -- 图片JSON数组(本地路径)
    original_images TEXT,          -- 原始图片URL JSON数组
    crawled_at TIMESTAMP,          -- 爬取时间
    post_date TEXT,                -- 原帖发布时间
    UNIQUE(source, source_id)      -- 防重复
);
```

### tasks 表
```sql
CREATE TABLE tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_type TEXT NOT NULL,        -- by_date / by_page
    params TEXT NOT NULL,           -- 任务参数JSON
    status TEXT DEFAULT 'pending',  -- pending/running/completed/failed/cancelled
    total_posts INTEGER DEFAULT 0, -- 总帖子数
    success_posts INTEGER DEFAULT 0, -- 成功爬取数
    skipped_posts INTEGER DEFAULT 0, -- 跳过数(无网盘链接)
    error_posts INTEGER DEFAULT 0, -- 失败数
    started_at TIMESTAMP,
    finished_at TIMESTAMP,
    created_at TIMESTAMP
);
```

## 四、各网站爬取策略

### 1. ACG游戏姬 (acgyxjvip.com)
- **列表页**: `https://www.acgyxjvip.com/page/{N}` (共2560页)
- **详情页**: `https://www.acgyxjvip.com/{id}.html`
- **互动数据**: 列表页直接有浏览量(`span.list-post-view`)和评论数(`span.list-post-comment`)
- **图片**: 懒加载，从 `data-src` 属性提取
- **分页**: `/page/N` 格式
- **按日期爬取**: 需要从详情页获取发布日期，列表页无日期信息

### 2. 萌幻ACG (bbs.acgrx.com)
- **列表页**: `https://bbs.acgrx.com/page/{N}` (共596页)
- **详情页**: `https://bbs.acgrx.com/game/{id}.html` 等
- **互动数据**: 列表页无任何互动数据，详情页可能有
- **图片**: 懒加载，从 `data-original` 属性提取
- **登录**: 需要自动登录才能查看详情页内容
- **分类**: 游戏(game)/动画(anime)/漫画(manga)等，只爬游戏分类
- **按日期爬取**: 列表页有 `data-timestamp` 属性

### 3. ACG图书馆 (acgll.xyz)
- **分类页**: `https://acgll.xyz/category/youxi/page/{N}` (共2283页)
- **详情页**: `https://acgll.xyz/{slug}`
- **互动数据**: 需AJAX加载或从分类页获取
- **图片**: 懒加载，从 `data-src` 属性提取
- **隐藏内容**: 跳过，只抓公开内容
- **按日期爬取**: 需从详情页获取发布日期

### 4. ACG俱乐部 (acgjlb.cc)
- **列表页**: `https://www.acgjlb.cc/acggame` (共12385篇)
- **分类**: 安卓直装/安卓模拟器/PC游戏/galgame/Steam游戏
- **标签**: SLG/RPG/ADV/ACT/3D/日系/欧美/国产
- **排序**: 发布/更新/浏览/点赞/评论/收藏
- **按日期爬取**: 需从详情页获取发布日期

### 5. 日期解析策略
各站点日期格式不统一，需要统一解析：
- ACG游戏姬: `2026年4月15日` 或 `2026-04-15`
- 萌幻ACG: `data-timestamp` Unix时间戳
- ACG图书馆: 需从详情页提取
- ACG俱乐部: 需从详情页提取

## 五、核心模块设计

### 1. 爬虫引擎
```python
class CrawlerEngine:
    """多线程爬虫引擎"""
    - max_workers: 8 (可配置)
    - proxy: http://127.0.0.1:7890
    - retry: 3次，指数退避
    - delay: 0.5~1.5秒随机
    - timeout: 15秒
    
    方法:
    - crawl_by_date(site, start_date, end_date)
    - crawl_by_page(site, start_page, end_page)
    - cancel()  # 取消当前任务
```

### 2. 链接提取器
```python
class LinkExtractor:
    """网盘链接提取"""
    - extract_baidu(text) -> (url, code)
    - extract_mobile(text) -> (url, code)
    - 过滤: 只保留百度和移动云盘
    - 忽略: 夸克、UC、迅雷等
```

### 3. 图片处理器
```python
class ImageHandler:
    """图片下载和管理"""
    - download_image(url, save_path) -> local_path
    - 批量下载，跳过已下载
    - 失败回退到原始URL
    - 图片去重(MD5)
```

### 4. HTML生成器
```python
class HTMLGenerator:
    """生成离线HTML"""
    - generate_pc_html(posts) -> PC.html
    - generate_mixed_html(posts) -> PC+安卓.html
    - 图片用相对路径引用
    - 内嵌CSS样式
```

## 六、前端界面设计

### 主要页面
1. **控制面板**: 选择站点、爬取模式、日期范围、页码范围
2. **进度面板**: 实时日志、进度条、当前状态
3. **结果面板**: 卡片网格、平台过滤、搜索
4. **历史面板**: 任务列表、查看/删除/导出

### 卡片设计
```
┌─────────────────────────────────────┐
│  [平台标签]  [来源站点]  [发布日期]  │
│                                     │
│  ┌─────────────────────────────┐   │
│  │         缩略图轮播           │   │
│  └─────────────────────────────┘   │
│                                     │
│  游戏标题 (保留原始【PC/安卓】前缀)  │
│                                     │
│  ❤ 128  💬 32  👁 1.2k            │
│                                     │
│  [百度网盘]  [移动云盘]  [原帖]     │
│  解压码: xxxxxxx                   │
│  作弊码: xxxxxxx (可点击复制)       │
│                                     │
│  [删除]                            │
└─────────────────────────────────────┘
```

## 七、配置文件 (config.yaml)

```yaml
# 代理设置
proxy:
  enabled: true
  http: "http://127.0.0.1:7890"
  https: "http://127.0.0.1:7890"

# 爬虫设置
crawler:
  max_workers: 8
  request_delay: [0.5, 1.5]  # 随机延迟范围(秒)
  timeout: 15
  max_retries: 3

# 萌幻ACG登录
acgrx:
  email: "Tsinhos@qq.com"
  password: "123456@@@@"

# 图片设置
images:
  download: true
  max_size: 10  # MB，超过不下载
  timeout: 30

# 网盘过滤
filter:
  allow_baidu: true
  allow_mobile: true
  allow_quark: false
  allow_uc: false
  allow_xunlei: false

# 排序
sort:
  by: "likes_comments"  # likes_comments / likes / random
  order: "desc"

# Flask
flask:
  host: "127.0.0.1"
  port: 5000
  debug: false
```

## 八、开发计划

### 阶段1: 基础框架 (第1天)
- [ ] 创建项目目录结构
- [ ] 实现 config.yaml 加载
- [ ] 实现 SQLite 数据库初始化
- [ ] 实现 Flask 基础路由

### 阶段2: 爬虫核心 (第2-3天)
- [ ] 实现基础爬虫类 (base.py)
- [ ] 实现 ACG游戏姬 爬虫
- [ ] 实现 萌幻ACG 爬虫 (含登录)
- [ ] 实现 ACG图书馆 爬虫
- [ ] 实现链接提取器
- [ ] 实现图片处理器

### 阶段3: 前端界面 (第4-5天)
- [ ] 实现主界面HTML
- [ ] 实现控制面板JS
- [ ] 实现进度展示(WebSocket或轮询)
- [ ] 实现结果卡片渲染
- [ ] 实现历史记录

### 阶段4: 导出功能 (第6天)
- [ ] 实现HTML生成器
- [ ] 实现PC.html导出
- [ ] 实现PC+安卓.html导出
- [ ] 实现start.bat

### 阶段5: 测试优化 (第7天)
- [ ] 各网站爬取测试
- [ ] 并发稳定性测试
- [ ] 错误处理完善
- [ ] 文档编写

## 九、待确认未决项
★ 无

## 十、关键数字与口径
- 目标网站: 5个 (ACG游戏姬、萌幻ACG、ACG图书馆、ACG俱乐部，IACG.RIP暂不爬取)
- 数据量: 近1个月
- 并发数: 8
- 请求延迟: 0.5~1.5秒随机
- 图片下载: 是，离线可用
- 代理: 127.0.0.1:7890

## 十一、产出物清单
- `【技术方案】交接文档.md` → 本文件
- `【需求分析】交接文档.md` → 需求确认记录
- `【项目架构】交接文档.md` → 项目结构说明

## 十二、下一步待办
1. 创建项目目录结构
2. 实现基础框架 (config, database, flask)
3. 从ACG游戏姬爬虫开始实现

## 十三、风险与问题
暂无

## 十四、对话信息记录
### 14.1 用户告知的额外信息
- 用户本地已有Clash代理环境
- 用户主要爬取近几天到1个月的数据
- 用户不想太慢，希望较快的爬取速度
- IACG.RIP因Cloudflare防护暂不爬取

### 14.2 用户提出的要求或方法
- 图片需要离线可用，可以拖动到本地目录
- 萌幻ACG需要自动登录
- ACG图书馆跳过隐藏内容
- 排序按点赞+评论，萌幻ACG无数据则随机
- 作弊码/解压步骤放在卡片里，点击可复制
- start.bat按推荐方案实现

## 十五、更新记录
- 第 1 次对话 · 2026-09-06：创建技术方案文档，确认所有需求细节