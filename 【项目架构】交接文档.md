# 【项目架构】交接文档

> 最后更新：2026-09-07

## 一、项目目标

本地运行的ACG游戏资源聚合爬取工具，从多个站点抓取资源信息，提供Web界面预览，并支持导出离线HTML文件。

## 二、目标站点

| 序号 | 网站 | 域名 | CMS | 登录 | 状态 |
|------|------|------|-----|------|------|
| 1 | ACG游戏姬 | www.acgyxjvip.com | WordPress + Lolimeow | 不需要 | ✓ |
| 2 | 萌幻ACG | bbs4.acgrx.com | Typecho + MiKu | 需要 | ✓ |
| 3 | ACG图书馆 | acgll.xyz | WordPress + Zibll | 不需要 | ✓ |
| 4 | ACG俱乐部 | www.acgjlb.cc | WordPress + Zibll | 不需要 | ✓ |

备用域名：
- ACG游戏姬：www.acgyxjvip2.com
- 萌幻ACG：bbs.acgrx.com（主站）

## 三、技术栈

| 层 | 技术 |
|----|------|
| 前端 | HTML + CSS + 原生JavaScript（单页应用） |
| 后端 | Python + Flask |
| 数据库 | SQLite（data/crawler.db） |
| 爬虫 | requests + BeautifulSoup4 + lxml |
| 图片 | 本地存储 + 连接池Session |

## 四、项目结构

```
acg_crawler/
├── app.py                 # Flask主程序，API路由定义
├── config.py              # 配置管理（读取YAML）
├── config.yaml            # 配置文件
├── requirements.txt       # Python依赖
├── start.bat              # Windows一键启动脚本
├── crawler/               # 爬虫模块
│   ├── __init__.py        # CrawlerEngine（调度器）
│   ├── base.py            # BaseCrawler（抽象基类）
│   ├── acgyxj.py          # ACG游戏姬爬虫
│   ├── acgrx.py           # 萌幻ACG爬虫（含登录）
│   ├── acgll.py           # ACG图书馆爬虫
│   └── acgjlb.py          # ACG俱乐部爬虫
├── parser/                # 解析模块
│   ├── __init__.py        # 链接提取、云盘名提取、作弊码提取
│   └── image_handler.py   # 图片下载（连接池、多线程、重试）
├── database/              # 数据库模块
│   └── __init__.py        # SQLite CRUD操作
├── generator/             # 导出模块
│   └── __init__.py        # HTML生成 + zip打包
├── templates/
│   └── index.html         # 主页面模板
├── static/
│   ├── app.js             # 前端逻辑
│   └── style.css          # 样式
├── data/
│   └── crawler.db         # SQLite数据库文件
├── images/                # 下载的图片（按post_id分目录）
│   └── {post_id}/
│       └── {hash}.webp
└── output/                # 导出目录
    ├── PC下载/
    │   ├── PC下载.html
    │   └── images/
    └── PC+安卓下载/
        ├── PC+安卓下载.html
        └── images/
```

## 五、核心模块说明

### 5.1 爬虫模块 (crawler/)

**BaseCrawler** (base.py)：
- 抽象基类，定义爬虫接口
- 内置 `_soup()` 方法（SSL验证关闭、超时、重试）
- 内置 `_fetch_with_retry()` 方法（指数退避重试）

**ACGYXJCrawler** (acgyxj.py)：
- 主域名失败自动切换备用域名
- 标题格式化：`[新作/类型] 游戏名 [平台 大小] [云名]`
- 云名清理：`PCC152888` → `C155555`（去除PC前缀）
- 统计提取：浏览量(span.view)、点赞(span.like-count)、评论(single-meta-box文本)
- 平台检测：优先从标题判断，避免分类误判

**ACGRXCrawler** (acgrx.py)：
- 需要登录（邮箱+密码）
- 登录后保持Session
- 解压码从正文提取（正则匹配）

**ACGLLCrawler** (acgll.py)：
- 无解压码（固定为空）
- 图懒加载：`data-src` 属性

**ACGJLBCrawler** (acgjlb.py)：
- 列表选择器：`div.item-body > h2.item-heading > a`
- 过滤置顶帖（根据标题关键词）
- 固定解压码：`007721`

### 5.2 图片处理 (parser/image_handler.py)

- 共享 `requests.Session`（连接池：20连接）
- 下载顺序：直连 → 代理 → 无Referer直连
- 文件名：URL的MD5哈希 + 原始扩展名
- 最小文件：500字节（小于视为失败）
- 并发下载：`max_workers=6`

### 5.3 导出模块 (generator/)

- HTML模板：暗色主题、卡片网格布局
- 图片：复制到output目录，使用相对路径引用
- 打包：zip格式（HTML + images目录）
- 平台分类：PC / PC+安卓（android归入pc_android）

### 5.4 数据库 (database/)

**posts表**：
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| source | TEXT | 来源站点 |
| source_id | TEXT | 原帖ID |
| source_url | TEXT | 原帖链接 |
| title | TEXT | 标题 |
| platform | TEXT | 平台：pc/android/pc_android |
| content | TEXT | 正文内容 |
| likes | INTEGER | 点赞数 |
| comments | INTEGER | 评论数 |
| views | INTEGER | 浏览量 |
| unzip_code | TEXT | 解压码 |
| cheat_code | TEXT | 作弊码 |
| baidu_link | TEXT | 百度网盘链接 |
| baidu_code | TEXT | 百度提取码 |
| mobile_link | TEXT | 移动云盘链接 |
| mobile_code | TEXT | 移动提取码 |
| images | TEXT | 图片路径（JSON数组） |
| original_images | TEXT | 原始图片URL |
| crawled_at | TIMESTAMP | 爬取时间 |
| post_date | TEXT | 发布时间 |

**tasks表**：记录爬取任务历史

## 六、配置说明 (config.yaml)

```yaml
proxy:
  enabled: false           # 是否启用代理
  http: "http://127.0.0.1:7890"

crawler:
  max_workers: 12          # 并发爬取数
  request_delay_min: 0.2   # 最小请求间隔(秒)
  request_delay_max: 0.5   # 最大请求间隔(秒)
  timeout: 10              # 请求超时(秒)

acgrx:                     # 萌幻ACG登录信息
  email: "Tsinhos@qq.com"
  password: "(已迁移到.env本地文件，不入git)"

flask:
  host: "127.0.0.1"
  port: 5000
  debug: true
```

## 七、API接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/` | GET | 主页面 |
| `/api/posts` | GET | 获取帖子列表（支持platform/source筛选） |
| `/api/posts_grouped` | GET | 按平台分组获取 |
| `/api/start_crawl` | POST | 启动爬取任务 |
| `/api/stop_crawl` | POST | 停止爬取任务 |
| `/api/progress` | GET | 获取爬取进度和日志 |
| `/api/export` | GET | 执行导出 |
| `/api/export_download` | GET | 下载导出zip |
| `/api/export_counts` | GET | 获取各平台导出数量 |
| `/api/redownload_images` | POST | 重新下载所有图片 |
| `/api/delete_post` | POST | 删除单个帖子 |
| `/api/batch_delete` | POST | 批量删除 |
| `/api/proxy_image` | GET | 图片代理 |
| `/images/{post_id}/{file}` | GET | 本地图片 |

## 八、已知问题与注意事项

1. **评论数据**：所有站点评论/浏览量已移除，仅保留点赞数
2. **图片下载**：需要网络正常，CDN图片通常可直连
3. **代理**：config.yaml中默认开启，Clash未运行时需关闭或直连
4. **ACG俱乐部**：固定解压码007721
5. **置顶帖过滤**：各站通过标题关键词过滤（教程、合集、工具、必看等）
6. **标题处理**：`fix_title_tags()`自动检测额外标签移到开头【】，`fix_title_brackets()`统一【】括号

## 九、后续可优化项

- [ ] 添加更多网盘支持（夸克、UC）
- [ ] 实现增量爬取（检测已爬取内容）
- [ ] 添加爬取速率动态调整
- [ ] 支持导出为单文件HTML（base64内嵌图片）
- [ ] 添加数据统计面板

## 十、更新记录

### 2026-09-07 v2
- `fix_title_tags()`: 标签自动检测（官中版、AI汉化、步兵版等）移到开头【】
- `fix_title_brackets()`: 所有标题[]统一改为【】
- 图片高度: 160→250px（CSS + 导出模板）
- 解压码/作弊码: 改为按钮，点击复制"解压码：xxx"
- 双网盘排序: 所有双网盘置顶，移除"低热度下沉"
- 置顶帖过滤: 各站skip_keywords扩展
- 残留斜杠: 【PC/3.87G】→【PC 3.87G】
- 萌幻ACG: 标题自动补平台到【】
- 移动云盘正则: 支持完整URL含/#/
- DB同步: 801条标题全部统一为【】
- 导出修复: copyText正确读取data-copy属性
- 导出修复: 全平台导出返回PC+安卓文件
- 数据库修复: get_posts_by_crawl_id排序和平台过滤与主查询一致

## 十一、联系方式

**项目负责人**: Tsinho
**联系邮箱**: Tsinhos@qq.com
